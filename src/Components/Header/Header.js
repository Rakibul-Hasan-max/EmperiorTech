import React from 'react';

const Header = () => {
    return (
        <div>
            <h1>this is header</h1>
            <button>Home</button>
            <button>About</button>
            <button>Services</button>
            <button>Blogs</button>
        </div>
    );
};

export default Header;